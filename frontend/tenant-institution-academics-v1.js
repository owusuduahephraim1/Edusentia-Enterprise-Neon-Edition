(()=>{
  "use strict";
  if(window.EDS_INSTITUTION_ACADEMICS_V1)return;
  window.EDS_INSTITUTION_ACADEMICS_V1=true;
  // Compatibility marker only. Institution-specific academic operations are handled
  // by the Worker-backed certified academics workspace in the Neon Edition.
})();